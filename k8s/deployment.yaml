apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-suite-v3
  labels:
    app: microservice-suite-v3
spec:
  replicas: 3
  selector:
    matchLabels:
      app: microservice-suite-v3
  template:
    metadata:
      labels:
        app: microservice-suite-v3
    spec:
      containers:
      - name: api
        image: microservice-suite-v3:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:postgres@db:5432/app"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
